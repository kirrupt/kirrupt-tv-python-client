A Python client for the Kirrupt TV API.

# Installation

You can install via pip.

    $ pip install kirrupt_tv

Or via source code / GitHub.

    $ git clone https://github.com/kirrupt/kirrupt-tv-python-client kirrupt_tv
    $ cd kirrupt_tv
    $ python setup.py install
